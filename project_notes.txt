# set up virtual envr
python -m venv 21_env 
source 21_env/bin/activate
pip install --upgrade pip 

# streamlit
pip install streamlit
streamlit hello # show demo

streamlit run streamlit_app.py 

# from jupyerlab in posit cloud
pip install streamlit
touch streamlit_app.py
streamlit run streamlit_app.py 

pip install rsconnect-python

rsconnect deploy notebook \
    --server https://connect.example.org:3939 \
    --api-key my-api-key \
    streamlit_app.py 

pip freeze > requirements.txt
pip install -r requirements.txt

# https://pypi.org/project/rsconnect-python/
rsconnect deploy streamlit -n posit.cloud --entrypoint streamlit_app.py 



rsconnect add \
          --account '43tih9-alex-chisholm' \
          --name 'posit.cloud' \
          --token '{TOKEN}' \
          --secret '{SECRET}' \
          --server 'posit.cloud'

# shinyapps.io

rsconnect list
rsconnect remove --name 43tih9-alex-chisholm



rsconnect add \
	  --account 43tih9-alex-chisholm \
	  --name 43tih9-alex-chisholm \
          --token '{TOKEN}' \
          --secret '{SECRET}' \
      --server 'shinyapps.io'

# local
rsconnect deploy streamlit /Users/alexchisholm/Desktop/Coding/streamlit --exclude 20_env --exclude project_notes.txt  --entrypoint streamlit_app.py --new

rsconnect deploy streamlit /Users/alexchisholm/Desktop/Coding/streamlit --exclude 20_env --exclude project_notes.txt  --entrypoint streamlit_app.py 


'Error: Unable to infer Connect server type and setup server.'

# cloud: fails
rsconnect deploy streamlit /cloud/project --entrypoint streamlit_app.py

